{% if helpers.exists('HWasly.vnstat.general.enabled') and HWasly.vnstat.general.enabled == '1' %}
{% from 'HWasly/Macros/interface.macro' import physical_interface %}
vnstat_setup="/usr/local/hwasly/scripts/HWasly/Vnstat/setup.sh"
vnstat_enable="YES"
{%   if helpers.exists('HWasly.vnstat.general.interface') and HWasly.vnstat.general.interface != '' %}
{%     set interfaces = [] %}
{%     for interface in HWasly.vnstat.general.interface.split(',') %}
{%       do interfaces.append(physical_interface(interface)) %}
{%     endfor %}
vnstat_additional_ifaces="{{ interfaces|join(' ') }}"
{%   endif %}
{% else %}
vnstat_enable="NO"
{% endif %}
