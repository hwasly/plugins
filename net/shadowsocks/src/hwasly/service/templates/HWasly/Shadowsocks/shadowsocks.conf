{% if helpers.exists('HWasly.shadowsocks.general.enabled') and HWasly.shadowsocks.general.enabled == '1' %}
{
    "server":"{{ HWasly.shadowsocks.general.serveraddress }}",
    "server_port":{{ HWasly.shadowsocks.general.serverport }},
    "local_port":{{ HWasly.shadowsocks.general.localport }},
    "password":"{{ HWasly.shadowsocks.general.password }}",
    "timeout":60,
    "method":"{{ HWasly.shadowsocks.general.cipher }}"
}
{% endif %}
