# Please don't modify this file as your changes might be overwritten with
# the next update.
#
{% if helpers.exists('HWasly.Rspamd.general.enabled') and HWasly.Rspamd.general.enabled == '1' and helpers.exists('HWasly.Rspamd.spf') %}

    spf_cache_size = {{ HWasly.Rspamd.spf.spf_cache_size|default("2") }}k;
    spf_cache_expire = {{ HWasly.Rspamd.spf.spf_cache_expire|default("1") }}d;

{% endif %}
