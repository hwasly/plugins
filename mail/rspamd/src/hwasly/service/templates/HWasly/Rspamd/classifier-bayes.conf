# Please don't modify this file as your changes might be overwritten with
# the next update.
#

{% if helpers.exists('HWasly.Rspamd.general.enabled') and HWasly.Rspamd.general.enabled == '1' and helpers.exists('HWasly.Rspamd.general.enable_bayes_autolearn') %}

{% if helpers.exists('HWasly.Rspamd.general.enable_bayes_autolearn') and HWasly.Rspamd.general.enable_bayes_autolearn == '1' %}
        autolearn = true;
{% endif %}

{% endif %}
