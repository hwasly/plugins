{% if helpers.exists('HWasly.Rspamd.general.enabled') and HWasly.Rspamd.general.enabled == '1' and helpers.exists('HWasly.Rspamd.surbl.exceptions') and HWasly.Rspamd.surbl.exceptions != '' %}
{%   for host in HWasly.Rspamd.surbl.exceptions.split(',') %}
{{ host }}
{%   endfor %}
{% endif %}
