# Please don't modify this file as your changes might be overwritten with
# the next update.
#

{% if helpers.exists('HWasly.Rspamd.general.enabled') and HWasly.Rspamd.general.enabled == '1' and helpers.exists('HWasly.Rspamd.dkim') %}

    dkim_cache_size = {{ HWasly.Rspamd.dkim.cache_size|default('2') }}k;
    dkim_cache_expire = {{ HWasly.Rspamd.dkim.cache_expire|default('1') }}d;
    time_jitter = {{ HWasly.Rspamd.dkim.time_jitter|default('6') }}h;
    trusted_only = {% if helpers.exists('HWasly.Rspamd.dkim.trusted_only') and HWasly.Rspamd.dkim.trusted_only == '1' %}true{% else %}false{% endif %};
    skip_multi = {% if helpers.exists('HWasly.Rspamd.dkim.skip_multi') and HWasly.Rspamd.dkim.skip_multi == '1' %}true{% else %}false{% endif %};

{% endif %}
