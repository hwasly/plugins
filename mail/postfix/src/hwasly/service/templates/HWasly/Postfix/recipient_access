{% if helpers.exists('HWasly.postfix.general.enabled') and HWasly.postfix.general.enabled == '1' %}
{%   if helpers.exists('HWasly.postfix.recipient.recipients.recipient') %}
{%     for recipient_list in helpers.toList('HWasly.postfix.recipient.recipients.recipient') %}
{%       if recipient_list.enabled == '1' %}
{{ recipient_list.address }} {{ recipient_list.action }}
{%       endif %}
{%     endfor %}
{%   endif %}
{% endif %}
