{% if helpers.exists('HWasly.postfix.general.enabled') and HWasly.postfix.general.enabled == '1' %}
{%   if helpers.exists('HWasly.postfix.address.addresses.address') %}
{%     for address in helpers.toList('HWasly.postfix.address.addresses.address') %}
{%       if address.enabled == '1' %}
{{ address.from }} {{ address.to }}
{%       endif %}
{%     endfor %}
{%   endif %}
{% endif %}
